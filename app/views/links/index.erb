<h1> Links </h1>

<a href="/links/new">Add bookmark</a>

<ul id='links'>
  <%@links.each do |link| %>
  <li>
    Title: <%= link.title %>
    URL: <%= link.url %>
    Tags: <%= link.tags.first.name unless link.tags.empty? %>
  </li>
  <% end %>
</ul>
