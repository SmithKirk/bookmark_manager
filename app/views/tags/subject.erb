<h1><%= @links.tag %></h1>

<a href="/links">Back to bookmarks</a>

<ul id='links'>
  <% @links.each do |link| %>
  <li>
    Title: <%= link.title %>
    URL: <%= link.url %>
  </li>
  <% end %>
</ul>
